{
  "name": "knowledgehubai",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client": "npm run dev --workspace=client",
    "server": "npm run dev --workspace=server"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
